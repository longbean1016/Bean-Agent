# LLM 与嵌入客户端
openai>=2.0.0

# SKILL.md YAML frontmatter 安全解析
PyYAML>=6.0.2

# Web 服务
fastapi>=0.134.0
uvicorn>=0.41.0
websockets>=15.0

# Windows 不自带 IANA 时区数据库，ZoneInfo("Asia/Shanghai") 依赖此包。
tzdata>=2025.2

# 网页搜索、抓取与 HTML 内容转换
httpx>=0.28.0
html2text>=2025.4.15
lxml>=5.3.0

# 异步文件操作
aiofiles>=25.1.0

# 图片校验、EXIF 旋转、缩放和大图压缩
Pillow>=11.0.0

# 向量检索
sqlite-vec>=0.1.6

# Windows Restricted Token、ACL 与 Job Object 沙箱
pywin32>=312; sys_platform == "win32"

# 模型能力快照，用于在未显式配置时解析上下文窗口
litellm>=1.95.0,<2
